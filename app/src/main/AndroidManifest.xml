<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.amychau.multiplechoicetest">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MultipleChoice" />
        <activity android:name=".TrueFalseActivity" />
        <activity android:name=".ShortAnswerActivity" />
        <activity android:name=".QuizCreaterMain" />
        <activity android:name=".Questions" />
        <activity android:name=".QuestionDetails" />
        <activity android:name=".quizCreator" />
        <activity android:name=".statistics"/>
        <activity
            android:name=".Toolbar"
            android:label="@string/title_activity_toolbar" />
        <activity android:name=".importQuestion" />
        <activity android:name="quizActivty.QuizActivity"></activity>
    </application>

</manifest>